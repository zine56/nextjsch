// components/Loader.js
import React from 'react';
import styles from './Loader.module.css'; // Asume que tienes estilos CSS para el loader

const Loader = () => {
  return (
    <div className={styles.loader}>
      Cargando...
    </div>
  );
};

export default Loader;
